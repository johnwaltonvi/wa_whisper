#!/usr/bin/env bash
set -euo pipefail
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")"/.. && pwd)"
LOG_DIR="${WA_WHISPER_LOG_DIR:-$HOME/.cache/wa_whisper}"
LOG_FILE="$LOG_DIR/service.log"
mkdir -p "$LOG_DIR"
exec "$ROOT_DIR/scripts/wa-whisper" "$@" >>"$LOG_FILE" 2>&1

#!/usr/bin/env bash
set -euo pipefail
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")"/.. && pwd)"
VENV_DIR="$ROOT_DIR/.venv"
EXEC="$VENV_DIR/bin/wa-whisper"
if [[ ! -x "$EXEC" ]]; then
  echo "wa-whisper entrypoint missing at $EXEC. Run scripts/bootstrap.sh." >&2
  exit 1
fi
exec "$EXEC" "$@"
